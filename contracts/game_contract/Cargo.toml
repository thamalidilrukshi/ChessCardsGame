[package]
name = "game_contract"
version = "0.1.0"
edition = "2021"

[dependencies]
linera-sdk = "0.11.0"
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
thiserror = "1.0"

[dev-dependencies]
linera-sdk = { version = "0.11.0", features = ["test"] }
